# 6039_PMSM_STO_PLL_V0 代码解读索引

本文与源码内新增的中文注释配套，覆盖 Keil 工程当前实际编译的 39 个 C 文件。阅读时不要把
文件名中的 `stm32f4xx` 或 `F4xx` 当成当前硬件平台：这些名称来自 MCSDK 5.4.4 的目录结构，
本工程的 PWM、ADC 和中断实现已经适配 ET6039。

## 一、控制链路

```text
main.c
  ├─ 初始化 ET6039 ADC/SRPWM/GPIO/UART
  ├─ MX_MotorControl_Init → MCboot
  └─ MC_StartMotor1 → 只提交启动命令
                         │
SysTick/软件调度          ▼
  └─ TSK_MediumFrequencyTaskM1
       ├─ 状态机与开环 Rev-Up
       ├─ STO-PLL 收敛/可靠性判断
       └─ 速度 PI → Iq参考
                         │
PWM同步ADC中断（16 kHz）  ▼
  └─ TSK_HighFrequencyTask
       ├─ 读取两相电流并重构第三相
       ├─ Clarke：三相 → alpha/beta
       ├─ Park：alpha/beta → d/q
       ├─ Id/Iq PI → Vd/Vq
       ├─ 电压圆限幅
       ├─ 反Park → Valpha/Vbeta
       ├─ SVPWM → 三相比较值
       └─ STO-PLL：电压+电流 → 反电势、速度、角度
```

速度环是外环，电流环是内环。速度环频率较低，只负责产生转矩电流 `Iq_ref`；电流环每个PWM
周期运行，直接决定逆变器电压。无感启动早期没有足够反电势，先使用虚拟角度和强制速度，
STO连续可靠后才切换到估算角度。

## 二、运行状态

- `IDLE`：PWM关闭，允许接受启动。
- `OFFSET_CALIB`：电机无电流时测量三相采样零偏。
- `CHARGE_BOOT_CAP`：需要时给高侧驱动自举电容充电。
- `START`：执行开环多阶段升速，STO同时后台估算。
- `START_RUN`：虚拟角度与STO角度过渡。
- `RUN`：速度闭环和电流闭环均工作。
- `ANY_STOP/STOP/STOP_IDLE`：关PWM并等待转子/功率级稳定。
- `FAULT_NOW`：至少一个当前故障仍存在，PWM强制关闭。
- `FAULT_OVER`：当前条件已消失，但历史故障等待确认。

故障恢复必须遵循 `FAULT_NOW → FAULT_OVER → Acknowledge → IDLE → Start`，不能只重复调用
`MC_StartMotor1()`。

## 三、Application 文件

### 硬件和应用

- `main.c`：初始化、按键调速和前台指示；不执行实时FOC。
- `init_config.c`：ET6039 ADC、SRPWM、GPIO、UART及中断优先级。
- `mc_it.c`：ADC/PWM/UART/SysTick/HardFault中断入口。
- `mc_parameters.c`：六个SVPWM扇区的双ADC采样通道和结果寄存器表。
- `key.c`：按键扫描与消抖。

### MCSDK组织层

- `motorcontrol.c`：中间件统一初始化入口。
- `mc_config.c`：实例化PI、STO、Rev-Up、PWM、传感器、状态机等对象。
- `mc_tasks.c`：高频FOC、中频状态机、低频安全任务。
- `mc_api.c`：应用调用的启动、停止、斜坡和故障API。
- `mc_interface.c`：异步命令缓冲和命令状态。
- `mc_math.c`：Clarke/Park、反Park和定点三角函数。
- `regular_conversion_manager.c`：在不干扰同步电流采样的前提下调度普通ADC。

### 调试通信

- `motor_control_protocol.c`：协议寄存器到MCSDK变量/对象的映射。
- `user_interface.c`：通用UI对象和寄存器读写。
- `ui_task.c`：串行UI的初始化和周期任务。
- `dac_ui.c`：调试DAC板级适配。

## 四、MCSDK 控制算法文件

- `pid_regulator.c`：定点PI/PID、积分限幅、输出饱和和anti-windup。
- `circle_limitation.c`：把不可实现的d-q电压矢量压回允许圆内。
- `speed_torq_ctrl.c`：速度模式/转矩模式切换和外环Iq参考生成。
- `speed_pos_fdbk.c`：速度传感器公共量纲、角度和可靠性基类。
- `virtual_speed_sensor.c`：开环机械速度和电角度积分器。
- `revup_ctrl.c`：多阶段开环启动与STO收敛条件。
- `sto_pll_speed_pos_fdbk.c`：反电势状态观测器、PLL估速估角和可靠性检测。
- `state_machine.c`：合法状态迁移、当前故障和历史故障锁存。
- `ramp_ext_mngr.c`：速度/转矩等目标的扩展精度线性斜坡。
- `open_loop.c`：无感启动期的开环幅值生成。

## 五、PWM、电流和保护文件

- `pwm_curr_fdbk.c`：硬件无关PWM/电流抽象及SVPWM扇区/比较值计算。
- `pwm_common.c`：PWM实例的公共同步状态。
- `r3_2_f4xx_pwm_curr_fdbk.c`：ET6039三电阻双ADC采样、偏置校准、采样点移动、PWM开关。
- `bus_voltage_sensor.c`：母线电压传感器基类。
- `r_divider_bus_voltage_sensor.c`：电阻分压采样、滤波、欠压/过压和滞回。
- `ntc_temperature_sensor.c`：NTC滤波、过温和恢复滞回。
- `digital_output.c`：极性无关的数字保护/控制输出。

三电阻采样并不意味着每周期同时使用三相ADC值。SVPWM的某些相在特定区间紧贴母线，开关
瞬态会污染采样，因此软件按扇区选择两相可靠电流，并用 `Ia + Ib + Ic = 0` 重构第三相。

## 六、测量与通信文件

- `motor_power_measurement.c`：功率测量基类。
- `pqd_motor_power_measurement.c`：由d-q电压电流估算有功功率。
- `frame_communication_protocol.c`：帧缓冲、超时、校验和回调基类。
- `usart_frame_communication_protocol.c`：USART逐字节非阻塞收发状态机。
- `ui_irq_handler.c`：UI中断编号到对象回调的分发。
- `dac_common_ui.c`：选择内部变量并缩放到调试DAC通道。

## 七、主要参数从哪里修改

- 电机极对数、R、L、反电势：`Application/Inc/pmsm_motor_parameters.h`
- PD6010B采样电阻、运放增益、母线分压：`Application/Inc/power_stage_parameters.h`
- PWM、PI、STO、启动阶段和保护阈值：`Application/Inc/drive_parameters.h`
- 时钟、死区、采样窗口的数字换算：`Application/Inc/parameters_conversion.h`
- ET6039引脚、ADC、SRPWM实例：`Application/Inc/init_config.h` 与 `Application/Src/init_config.c`

修改参数时应保持单位和缩放一致。尤其不能只因示波器波形不理想就直接改PI：先验证PWM极性、
相序、ADC零偏、电流正负号、母线电压比例和电机参数，否则PI调节无法补偿硬件映射错误。

## 八、实时性和安全约束

1. ADC/PWM中断中禁止阻塞、动态内存、文件操作和printf。
2. 电流采样校准时必须确保相电流为零。
3. 欠压、过压、过温和过流保护不应为方便调试而关闭。
4. 修改PWM频率后必须同步检查控制频率、PI离散增益、死区和ADC采样窗口。
5. 修改U/V/W引脚或电机相线后必须同时核对相电流通道、扇区表和方向。
6. 无感STO在低速/零速不可观，不能把“跳过低速”简单理解为控制缺陷。

